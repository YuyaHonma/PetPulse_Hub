<%= form_with(model: [post, @advice], url: trainer_post_advices_path(post), local: true) do |f| %>
  <%= f.text_area :advice, rows: '5', placeholder: "コメントをここに", class: "w-100" %>
  <%= f.submit "送信" %>
<% end %>
